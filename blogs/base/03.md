---
title: 03-JS基础
date: 2028-12-15
sticky: 1
sidebar: 'auto'
tags:
 - js
categories:
 - base
---

## 一，计算机组成

### 1，基本概念

**计算机的组成：**

* 计算机是由多个硬件组合而成，常见的硬件有：CPU、硬盘、内存、网卡、显示器、机箱、电源....

* 机械将零件组合在一起，他们之间是无法进行协作。

  

![1693444283082](./assets/1693444283082.png)

**操作系统：**

* 用于协调计算机的各个硬件，让硬件之间进行协同工作，以完成某个目标。
* 常见的操作系统
  + windows，优点：生态牛逼、工具多；缺点：略慢、收费。【个人】
  + linux，优点：资源占用少、免费（很多公司的服务器上安装Linux）；缺点：工具少、告别游戏。【企业的服务器】
  + mac，优点：生态还行、工具差不多都有、用户体验和交互； 缺点：不能玩游戏

**应用软件：**

* 在安装上操作系统之后，我们会在自己电脑上安装一些常用的软件，例如：QQ、杀毒、微信...
* 问题来了：这些软件是由谁开发的？是由各大公司的程序员开发的。
* 前端开发主要开发浏览器上运行的应用程序，可以把软件理解成为一大堆的代码的集合。

![1693444729081](./assets/1693444729081.png)

**编程语言：**

* 软件，是由程序员使用 编程语言 开发出来的一大堆代码的集合。全球的编程语言有2500+多种，常见的编程语言：JavaScript、Java、C#、Python、PHP、C...

* 学习编程语言就是学习它的语法，根据语法再去编写相应的软件中的功能。

  

![1693444905204](./assets/1693444905204.png)

**编程语言分类：**

* 从翻译的角度来说，分编译型和解释型
* 从高低的角度来说，分低级语言和高级语言

**低级语言和高级语言：**

* 低级编程语言，写出来的代码直接可以被计算机识别。

  + 机器语言：101 001 00001 00010 001000100010001，机器码，交给计算机去执行。
* 汇编语言，MOV INC ... ，指令，交给计算机去执行。
* 高级编程语言，写出来的代码无法直接被计算机识别，但可以通过某中方式将其转换为计算机可以识别的语言。
  + JS、C、C++、Java、Python、Ruby...，这类编程语言在编写代码时几乎是写英语作文。
  + 交由相关编译器或解释器翻译成机器码，然后再交给计算机去执行。

**编译器和解释器：**

* 编程语言分编译型的和解释型器，编译型的语言是一次性编译好，解释型语言，边解释边执行。

* 编译器/解释器，就是一个 `翻译官` ，将代码翻译成计算机能够识别的命令。
* wc使用JS开发一个软件，写10000行代码，需要使用JS解释器，翻译成计算机可以识别的命令。
* xq使用Go开发一个软件，写20000行代码，需要使用GO编译器，翻译成计算机可以识别的命令。
* 解释器，实时翻译。拿到1000行代码之后，解释一句交给操作系统一句。
* 编译器，全文翻译。拿到2000行代码之后会将他编译成一个临时文件（计算机能够识别命令），再把文件交给操作系统去读取。  
* JavaScript、Python、PHP、Ruby... 一般称为解释型语言。
* C、C++、Go、Java... 一般称为编译型语言。

**学习编程的本质：**

* 第一步：选择一门编程语言，在自己电脑上安装此编程语言的编译器或解释器。
* 第二步：学习语言的规则，语法规则+业务背景 开发我们需要的软件（代码的集合）
* 第三步：使用编译器或解释器去运行自己写的代码。

**总结：**

1. 简述硬件 & 操作系统 & 软件（应用系统）之间的关系。
   - 计算机是由多个硬件组成，例如：CPU、硬盘、内存、网卡、主板等。
   - 操作系统则是安装在计算机上用于协调各硬件进行配合工作的，他将用户的一些行为转化为计算机能够识别的命令，并协调各个硬件配合完成相关命令。 
   - 软件，是由程序员开发并安装在操作系统的程序（本质上是一大堆的代码），例如：微信、QQ、金山毒霸等。
   
2. 列举常见的操作系统都有哪些。

   常用的操作系统有三类：
    - win：win7、win10、xp 等
    - linux：centos、ubuntu、redhat 等。
    - mac：Catalina、Mojave、Sierra 等。
   
3. 简述编译器和解释器的区别和作用。 

   - 编译器和解释器的作用是将各编程语言的代码进行翻译，从而使得计算机能够识别并执行。
   - 编译器，全文翻译，将代码编译成 `临时文件` ，再执行临时文件。
   - 解释器，实时翻译，对代码实行边解释边执行。

## 二，JavaScript介绍

### 1，JS介绍

**JS介绍：**

​	JS是直接可以运行在浏览器上的，因为浏览器内置了JS的解释，是一种高级语言，是解释性语言。也就是我们所谓的脚本语言，所谓的脚本语言，就是不需要编译，由解释器去执行。当然现在JS也可以运行在NODE端，后面说。

**JS作用：**

* Web开发

* 服务端开发（Node.js）

* 桌面程序（Electron）

* App

* 控制硬件

* 游戏开发

* ...

  

![1693447461610](./assets/1693447461610.png)

**JS组成：**

* ECMAScript 

  + 规定了js基础语法核心知识。

* DOM

  + DOM 操作文档，比如对页面元素进行移动、大小、添加删除等操作

* BOM

  + BOM 操作浏览器，比如页面弹窗，检测窗口宽度、存储数据到浏览器等等

  

![1693447723841](./assets/1693447723841.png)

**ECMAScript版本：**

![1693447594691](./assets/1693447594691.png)

### 2，书写JS的三种方式

行内写法，目前这种写法不推荐，了解，直接上代码：

![1693447879597](./assets/1693447879597.png)

总结行内写法的特点：

* 可以将单行或少量 JS 代码写在HTML标签的事件属性中（以 on 开头的属性），如：onclick
* 可读性差， 在html中编写JS大量代码时，不方便阅读；
* 特殊情况下使用

还有一种叫内部写法，把JS代码写在script标签中，如下：

![1693448050782](./assets/1693448050782.png)

总结内部写法的特点：

* 可以把多行JS代码写在script标签中
* 学习时，为了方便，直接把JS代码写在scirpt标签
* 推荐将JavaScript代码和编写位置放在body子元素的最后一行；

最后一种写法，是外部写法，需要单独创建一个JS文件，通过script标签引入这个文件，如下：

![1693448245843](./assets/1693448245843.png)

总结外部写法的特点：

* 需要通过script元素的src属性来引入JavaScript文件
* 在写项目时，通常都会使用外部写法，把JS代码放在一个单独的文件中，在页面中引入。
* 使用外部写法，script标签中就不能写其它代码了，写了也白了
* script标签不能写成单标签
* 推荐将JavaScript代码和编写位置放在body子元素的最后一行

### 3，JS的输出与输入

为了方便信息的输入输出，JS中提供了一些输入输出语句，其常用的语句如下：

![1693449184655](./assets/1693449184655.png)

注意：alert() 主要用来显示消息给用户，console.log() 用来给程序员自己看运行时的消息。

直接上代码：

![1693449375138](./assets/1693449375138.png)

![1693449463314](./assets/1693449463314.png)

**做几个练习：**

* 在控制台中输出你自己的姓名
* 在控制台中输出  春眠不觉晓， 出处闻啼鸟， 夜来风雨声， 花落知多少。

## 三，初识数据类型

软件运行流程：

1. 打开某个程序时，先从硬盘中把程序的代码加载到内存中
2. CPU执行内存中的代码

![1693450105316](./assets/1693450105316.png)

​	前面说过，使用编译语言是去开发软件的，软件上面的很多的数据，这些数据都是放在内存的。内存空间是有限，绝大部分的编程语言为了合理地使用内存空间，都会把使用到的数据进行分类，分成不同的类型，目的就是为了更加合理的使用内存空间。现在我大致了解一下JS中的数据类型。

### 1，数字类型

是Number，包含整数和小数，例如：年龄、存款、尺寸、手机号等。数据类型是支持 加/减/乘/除/取余/指数 等操作。直接上代码：

![1693450613117](./assets/1693450613117.png)

### 2，字符串

字符串，其实就是我们生活中的文本信息。例如：姓名、地址、自我介绍等。 字符串有一个特点，他必须由引号引起来，直接上代码：

![1693450963034](./assets/1693450963034.png)

### 3，布尔类型

布尔类型就两个值：true和false。 布尔类型对应的值只有两个，一个是ture，一个是false， 直接上代码：

![1693451235038](./assets/1693451235038.png)

### 4，数据类型转化

把一种数据类型转化成另一个数据类型，转为数字，代码：

![1693451559535](./assets/1693451559535.png)

转换成字符串：

![1693451688038](./assets/1693451688038.png)

转换成布尔值：

![1693451804500](./assets/1693451804500.png)

**练习题：**

* 计算数字50乘以10再除以5的商并输出

* 判断数字8是否大于10的结果并输出

* 计算数字30除以2得到的余数并输出

* 判断 字符串”admin”和”root”是否相等的结果并输出

* 判断 字符串666和数字666是否相等的结果并输出

* 看代码写结果（禁止运行代码）

```html
  <meta charset="utf-8">
  <script>
      console.log(Number("100") * 3)
      console.log(Number("123") + Number("88"))
      console.log(String(111) + String(222))
      console.log(String(111) * 3)
      console.log(Number("8") > 7)
      console.log(String(111) == 111)
      console.log(Boolean(-1))
      console.log(Boolean(0))
      console.log(Boolean(""))
      console.log(Boolean("你好"))
      console.log(true == true)
      console.log(true == false)
      console.log(Boolean("") == Boolean(0))
  </script>
```

  

参考答案：

* 计算数字50乘以10再除以5的商并输出
    console.log(50*10/5)

* 判断数字8是否大于10的结果并输出
         console.log(8>10)

* 计算数字30除以2得到的余数并输出
     console.log(30%2)

* 判断 字符串”admin”和”root”是否相等的结果并输出
     console.log('admin' == 'root')

* 判断 字符串666和数字666是否相等的结果并输出
    console.log(666 == '666')

* 看代码写结果（禁止运行代码）

  

```html
    <meta charset="utf-8">
    <script>
        console.log(Number("100") * 3) // 300 
        console.log(Number("123") + Number("88")) // 211
        console.log(String(111) + String(222)) // '111222'
        console.log(String(111) * 3) // '333'
        console.log(Number("8") > 7) // true
        console.log(String(111) == 111) // true
        console.log(Boolean(-1)) // true
        console.log(Boolean(0)) // false
        console.log(Boolean("")) // false
        console.log(Boolean("你好")) // true
        console.log(true == true) // true
        console.log(true == false) // false
        console.log(Boolean("") == Boolean(0)) // true
    </script>
```

  

## 四，变量

### 1，什么是变量

学习一门编程语言，编程语言主要是用于开发软件的，在一个软件中有很多数据都是动态变化的，如：

1. 比如购物车商品的数量、价格的计算等等；
2. 比如一首歌曲播放的时间、进度条、歌词的展示等等；
3. 比如微信聊天中消息条数、时间、语音的长度、头像、名称等等；
4. 比如游戏中技能的冷却时间、血量、蓝量、buff时间、金币的数量等等；

可以使用变量来表示一个可以变化的数据，变量，其实就是我们生活中起别名和外号，让变量名指向某个值。变量的声明和变量的赋值，如下：

![1693463338534](./assets/1693463338534.png)

画图如下：

![1693463462014](./assets/1693463462014.png)

再练习一下：

![1693463696231](./assets/1693463696231.png)

### 2，变量的命令规范

规范：

1. 第一个字符必须是一个字母、下划线（ _ ）或一个美元符号（ $ ）
2. 其他字符可以是字母、下划线、美元符号或数字
3. 变量名不能是JS中的关键字
   - https://developer.mozilla.org/zh-CN/docs/web/javascript/reference/lexical_grammar
4. 变量严格区分大小写

代码演示：

![1693464031034](./assets/1693464031034.png)

变量建议：

1. 多个单词使用驼峰标识或下划名命令
2. 赋值 = 两边都加上一个空格；
3. 一条语句结束后加上分号; 也有很多人的习惯是不加；
4. 变量应该做到见名知意；

代码演示：

![1693464222401](./assets/1693464222401.png)

练习题：

* 练习一：定义一些变量，保存自己的个人信息：比如姓名、年龄、身高、体重、爱好等等
* 练习二：定义一个变量name，赋值成wangcai。定义一个变量admin，将name赋值给admin
* 练习三：定义变量，保存两个数字，并且对两个变量的数字进行交换

参考答案：

![1693465632988](./assets/1693465632988.png)

### 3，变量与内存指向关系

**情况1：**

![1693465860158](./assets/1693465860158.png)

**情况2：**

![1693466066922](./assets/1693466066922.png)

在计算机的内存中创建一块区域保存字符串”malu"，name变量名则指向这块区域。然后又再内存中创建了一块域保存字符串”wangcai"，name变量名则指向”wangcai“所对应的空间，不再指向"malu"所对应的空间。"malu"所对应的内存空间，无人指向的数据会被标记为垃圾，由解释器自动化回收。

**情况3：**

![1693466289634](./assets/1693466289634.png)

在计算机的内存中创建一块区域保存字符串”malu"，name变量名则指向这块区域。new_name变量名指向name变量，因为被指向的是变量名，所以自动会转指向到name变量代表的内存区域。

**情况四：**

![1693466469432](./assets/1693466469432.png)

**情况五：**

![1693466561954](./assets/1693466561954.png)

**练习题：**

1. 看代码写结果
   

![1693466714470](./assets/1693466714470.png)

2. 按要求写代码
	

![1693466950112](./assets/1693466950112.png)

​	

3. 看代码写结果 
	

![1693467083481](./assets/1693467083481.png)

​	

4. 看代码写结果  
	

   

![1693467208860](./assets/1693467208860.png)

   

### 4，练习题

回顾一下输入：

![1693467321194](./assets/1693467321194.png)

练习：

1. 提示输入姓名，然后给姓名后面拼接一个"666",最终打印结果。
2. 提示输入 姓名/位置/行为，然后做拼接并打印：xx 在 xx 做 xx 。
3. 提示输入两个数字，计算两个数的和。

## 五，深入数据类型

### 1，数据类型介绍

**为什么需要数据类型？**

* 给不同的数据分配不同的内存空间，数据类型就是为了让我们合理地使用内存空间

**在 JavaScript 中有 8 种数据类型**:

* number     用于任何类型的数字：整数或浮点数。
* string    用于字符串：一个字符串可以包含 0 个或多个字符，所以没有单独的单字符类型。
* boolean   用于 true 和 false。
* undefined   用于未定义的值 —— 只有一个 undefined 值的独立类型。表示空
* null  表示空，表示对象的空。
* object  用于更复杂的数据结构。后面讲面向对象时会细讲。
* bigInt  后面说
* symbol  后面说

其中有5个基本数据类型：

![1693530256530](./assets/1693530256530.png)

**typeof介绍与typoef的返回值：**

* typeof 可用来获取检测变量的数据类型
* "undefined"表示值未定义; 
* "boolean"表示值为布尔值; 
* "string"表示值为字符串; 
* "number"表示值为数值; 
* "object"表示值为对象(而不是函数)或 null; 
* "function"表示值为函数; 
* "symbol"表示值为符号；

代码演示：

![1693530705150](./assets/1693530705150.png)

### 2，Number数据类型

number可以表示整数，也可以表示小数，如下：

![1693530827478](./assets/1693530827478.png)

数字number可以有很多操作，比如，乘法 、除法 /、加法 +、减法 - 等，如下：

![1693531358327](./assets/1693531358327.png)

number也有不同进制的表示法：

![1693531756719](./assets/1693531756719.png)

数据的表示范围：

![1693532066039](./assets/1693532066039.png)

isNaN，用于判断是否是一个NaN，是NaN结果是true，否则结果是false，如下：

![1693532190890](./assets/1693532190890.png)

### 3，String数据类型

一个项目中，字符串类型用的是最多的，如 人的姓名：“wangcai”。地址：“北京市”。简介：“一名前端程序员”；

**JavaScript 中的字符串必须被括在引号里，有三种包含字符串的方式:**

* 双引号  “malu”
* 单引号  ’malu‘
* 反引号  ``

代码演示：

![1693532596515](./assets/1693532596515.png)

转义字符，转义就是转变原有的意思，如下：

![1693532920376](./assets/1693532920376.png)

通过索引访问访问某个字符串中的某个字符，如下：

![1693533025712](./assets/1693533025712.png)

可以求字符串的长度，如下：

![1693533079059](./assets/1693533079059.png)

字符串拼接，如下：

![1693533151021](./assets/1693533151021.png)

### 4，Boolean数据类型

Boolean类型就是表示真假，如是否毕业. 是否有身份证. 是否购买车票. 是否成年人；取值就两个：一个是true，一个是false。如下：

![1693534459461](./assets/1693534459461.png)

### 5，Undefined数据类型

undefined 类型只有一个值，就是特殊值 undefined:

* 如果我们声明一个变量，但是没有对其进行初始化时，它默认就是undefined；

![1693534537568](./assets/1693534537568.png)

### 6，Object数据类型

object数据类型是复杂类型，前面讲的都是基本数据类型，往往可以表示一组数据，是其他数据的一个集合。代码演示：

![1693534655511](./assets/1693534655511.png)

### 7，Null数据类型

null也是一种数据类型，它对应的值只有一个，也是null。也是表示空。只不过它通常表示一个对象为空，所以在给一个对象进行初始化时，赋值为null，如下：

![1693534968984](./assets/1693534968984.png)

### 8，数据类型转化

把一种数据类型转化成另一种数据类型，分两类：

* 强制数据类型转化，需要写代码进行转化
* 隐式数据类型转化，不需要写代码进行转化，它会悄悄自动的转化

### 9，其它类型转String

其它类型转string

* 把number转成string
* 把boolean转成string
* 把und转成string

方式一：toString，代码如下：

![1693535554278](./assets/1693535554278.png)

方式二：String构造函数，代码如下：

![1693535720365](./assets/1693535720365.png)

方式三：+把其它类型转成字符串，代码如下：

![1693535950467](./assets/1693535950467.png)

总结：

* 强制类型转化：toString 或 String构造器
* 隐式类型转化：+

### 10，其它类型转Number

#### A）把字符串转化成number

把字符串转成number

* 如果字符串中都是数值，就可以正常转化，如："123" => 123
* 如果是一个空串，空串是""，转化后， "" => 0
* 如果字符串中不只有数字，转化后是NaN 如："hello123" => NaN

利用Number构造器转化，代码如下：

![1693536194527](./assets/1693536194527.png)

利用+或-进行转化，如下：

![1693536430977](./assets/1693536430977.png)

利用parseInt和parseFloat进行转化，如下：

![1693536712558](./assets/1693536712558.png)

#### B）把布尔转化成number

转化方式：

* Number构造器
* +- 
* 如果是true，转化成数字1
* 如果是false，转化成数字0

直接上代码：

![1693536877669](./assets/1693536877669.png)

#### C）把und转化成number

直接上代码：

![1693536937949](./assets/1693536937949.png)

#### D）把null转化成number

直接上代码：

![1693537046371](./assets/1693537046371.png)

### 11，其它类型转Boolean

#### A）把string类型转成boolean

直接上代码：

![1693537217161](./assets/1693537217161.png)

#### B）把number类型转成boolean

直接上代码：

![1693537271991](./assets/1693537271991.png)

#### C）把und类型转成boolean

直接上代码：

![1693537293600](./assets/1693537293600.png)

#### D）把null类型转成boolean

直接上代码：

![1693537308124](./assets/1693537308124.png)

总结：如些数据转成布尔类型会变成false?

答：空串，0，-0，NaN，und，null 会转化成false，其它的都会转化成true 这个结论非常重要

## 六，条件语句

**在日常生活中，也有三大流程，有：顺序流程，选择流程，循环流程。日常生活中是任何事件，都可以使用上面的三大流程解释。在计算机中，对应了三大结构，顺序结构，分支结构，循环结构。**

![1693538248540](./assets/1693538248540.png)

要实现所谓选择结构，有三种实现方案：

* if / if else / if elseif else
* 三元运算符
* switch语句

先看最简单的if语句，满足条件就执行某些代码，不满足就不执行，如下：

![1693538462655](./assets/1693538462655.png)

单个if语句的语法：

```js
// 括号内的条件为true时，进入大括号里执行代码
// 小括号内的结果若不是布尔类型时，会发生隐式转换转为布尔类型
// 数字 0、空字符串 “”、null、undefined 和 NaN 都会被转换成 false
// 其他值被转换为 true
if (条件) {
    满足条件要执行的代码
}
```

还有所谓的if else语句，如下：

![1693538602120](./assets/1693538602120.png)

if..else语句的语法，如下：

```js
if (条件) {
    满足条件要执行的代码
} else {
    不满足条件要执行的代码
}

if (条件) {
    条件成立之后的代码...
        条件成立之后的代码...
        条件成立之后的代码...
} else {
    条件不成立之后执行的代码...
        条件不成立之后执行的代码...
        条件不成立之后执行的代码...
}
```

做一个小案例，感受一下：请输入用户名，如果用户名是malu，控制台提示欢迎malu，否提示你输入的用户名不正确，如下：

![1693538919486](./assets/1693538919486.png)

### 1，基本条件语句

对于if语句 或 ifelse语句，常见的使用方式，总结一下。

情况1：

![1693539027918](./assets/1693539027918.png)

情况2：

![1693539058286](./assets/1693539058286.png)

情况3：

![1693539135626](./assets/1693539135626.png)

情况4：

![1693539260731](./assets/1693539260731.png)

情况5：

![1693539334573](./assets/1693539334573.png)

情况6：

![1693539438035](./assets/1693539438035.png)

**练习题：**

* 提示用户输入用户名和密码，用户名等于"admin"且密码等于"123"就输出登录成功；否则输出登录失败。
* 猜数字，提示用户输入一个数字，判断数字如果大于10，就输出猜错了；否则输出猜对了。
* 提示用户输入一个数字，判断是否为偶数，是偶数则输出 偶数，否则输出 奇数。

### 2，多条件语句

### 3，条件嵌套

### 4，练习题

**练习题：**

* 接收用户输入的数据，判断这个数据是偶数还是奇数，如果是偶数，输出yes，如果是奇数，输出no
* 接收用户输入的1~7数据，判断是星期几后，打印出来
* 接收用户输入的1~12的数据，判断是什么季节后输出
* 有三个变量：a, b, c  将变量升序排序后输出
* 判断一个年份是否是闰年
* 输入一个0到50的数，如果数小于10，需要在它前面补0。

## 七，while循环语句

### 1，基本循环语句

### 2，案例练习

### 3，break

### 4，continue
